<h1>Hamburgers and Handlebars!</h1>
<h2>Handleburgers!</h2>
<ul>
  {{#each handleburgers}}
    {{#unless sleepy}}
      {{> handleburgers/handleburger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>
<h2>handleburgers that are delicious!</h2>
<ul>
  {{#each handleburgers}}
    {{#if sleepy}}
      {{> handleburgers/handleburger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>
<h2>Add a Handleburger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Handleburger:</label>
    <input type="text" id="ca" name="name">
  </div>
  <div class="form-group">
    <label for="slee">Devoured?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devoured!<br>
    <input type="radio" name="sleepy" value="0"> Not Devoured
  </div>
  <button type="submit">Add Handleburger</button>
</form>